import { defineMessages } from "react-intl";

export default defineMessages({
  configurationJson: {
    id: "switches.configurationJson",
    defaultMessage: "Configuration JSON",
  },
  formPreview: {
    id: "switches.formPreview",
    defaultMessage: "Form preview",
  },
});
